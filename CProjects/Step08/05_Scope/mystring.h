#define MAXSTRLEN 200
int readln(char[], int);
char * findsubstring(char[], char[]);

/**
 * This is commented, because in 'mystringutils.c' it is a private function and
 * CANNOT be declared in a HEADER file.
 **/
// int searchstring(char[], char[]);
